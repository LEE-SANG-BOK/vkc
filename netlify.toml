[build]
  # Run build from repo root (relative), publish subfolder with index.html
  base = "."
  publish = "vkc-agents/landing"
  command = ""

# Optional: prevent caching during rapid A/B tests
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-store"
